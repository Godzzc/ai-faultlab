# Local Docker Compose defaults only.
# Copy to .env for local overrides if needed.
# Do not use these defaults as production credentials.

MYSQL_DATABASE=faultlab
MYSQL_USERNAME=faultlab
MYSQL_PASSWORD=faultlab123456
MYSQL_ROOT_PASSWORD=root123456

RABBITMQ_USERNAME=faultlab
RABBITMQ_PASSWORD=faultlab123456

MILVUS_MINIO_ACCESS_KEY=minioadmin
MILVUS_MINIO_SECRET_KEY=minioadmin
MILVUS_MINIO_API_PORT=9000
MILVUS_MINIO_CONSOLE_PORT=9001
